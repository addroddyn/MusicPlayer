<UserControl x:Class="MusicPlayer.Views.MainWindow.MainWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayer.Views.MainWindow"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="{Binding TextBody}"/>
        </Grid>
        <Grid Row="1">
            <TextBox Text="{Binding TextBox}"
                     />
        </Grid>
        <Grid Row="2">
            <Button Content="Press me" cal:Message.Attach="[Event Click] = [Action ChangeMessage(2)]"/>
        </Grid>
    </Grid>
</UserControl>
